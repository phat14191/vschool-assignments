vat chai  = require("chai");
var assert = chai.assert;

var check = require("../check.js");

describe("Check Data Types", function() {
  it("should contains more than one data types",function () {
    assert.deepequal(check(), );
  })
})
